1.SQL语句不熟，稍微复杂就不会
2.对于SQL不能只看不写，要多写代码多练习
3.对于转账的流程存在模糊的地方
4.Java基础的IO部分不是很清楚

1.数据库查询的时候要起别名
（你可以在MySQL的SQL语法中用关键字SCHEMA替代DATABASE） 示例 ：${schema.inte}.cust_payee
2.并不是每个下一步都需要定义接口，“下一页”“下一页”可以保存数据
3.短信验证码是存在session里的
4.session只要打开页面就会产生，不需要登录就可以使用
5.往数据库中插入值时不能写死，可以写在Java代码中